.env
.env.*
!.env.example

node_modules/
dist/
build/
out/
.next/
next-env.d.ts
coverage/
*.log
*.tsbuildinfo

# Prisma client is generated into the API app, not committed
apps/api/src/generated/

# Local agent tooling state (skill registry and its cache)
.atl/

.DS_Store
Thumbs.db
*.tmp
*.swp
.vscode/
.idea/

# BEGIN DAW — pipeline runtime, NOT committed (managed by DAW)
#   .daw-state.json  = which phase you are in; yours, not the repo's
#   .daw-paused/     = paused tickets
#   .daw-sessions/   = live session markers
#   .daw-journal.jsonl = transitions that landed; outlives the state file
.daw-state.json
.daw-paused/
.daw-sessions/
.daw-journal.jsonl
# END DAW
